<div id="container">
    <div class="jumbotron">
        <h1>Fighting Games Article Scraper</h1>
    </div>
    <div id="articles">
        <ol>
            {{#each article}}
                <li>
                    {{this.title}}
                    {{this.link}}
                    {{#if this.note}}
                    <p class="well">
                        {{this.note.comment}}
                        <div class="btn">   
                            <button data-id="{{this.note._id}}" id="deletenote">Delete Note</button>
                        </div>
                    </p>
                    {{/if}}
                    <br>
                    <form class="create-update-form" action="/{{this._id}}" method="POST">
                        <div class="form-group">
                            <input type="text" name="comment">
                        </div>
                        <div class="btn">   
                            <button type="submit" class="sub">Save Note</button>
                        </div>
                    </form>
                </li>
                <br>
            {{/each}}
        </ol>
    </div>
    <div id="notes">
    </div>
</div>
    